class Workshop {
    id: number;
    nome: string;
    dataRealizacao: Date;

    constructor(id: number, nome: string, dataRealizacao: Date) {
        this.id = id;
        this.nome = nome;
        this.dataRealizacao = dataRealizacao;
    }
}